# Default example exercises (comma-separated)
DEFAULT_GENERATION_EXAMPLES=one,two

# Default exercises for generation (comma-separated)
DEFAULT_GENERATION_EXERCISES=three,four,five

# Default exercises for distillation (comma-separated)
DEFAULT_DISTILLATION_EXERCISES=one,two,three,four,five
